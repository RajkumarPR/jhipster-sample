'use strict';

angular.module('ubwebApp')
    .factory('Cities', function ($resource) {
        return $resource('api/citiess/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
