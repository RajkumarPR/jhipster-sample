'use strict';

angular.module('ubwebApp')
    .factory('EventLocation', function ($resource) {
        return $resource('api/eventLocations/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
