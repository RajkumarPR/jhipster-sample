'use strict';

angular.module('ubwebApp')
    .factory('Enquiry', function ($resource) {
        return $resource('api/enquirys/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
