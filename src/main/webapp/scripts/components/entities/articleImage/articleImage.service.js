'use strict';

angular.module('ubwebApp')
    .factory('ArticleImage', function ($resource) {
        return $resource('api/articleImages/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
